<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="test1Xamrin.ProvincialCell"
             BindingContextChanged="Handle_BindingContextChanged">
    <ContentView.Content>
        <StackLayout Spacing="0" Padding="5,0,5,0">
            <Grid Padding="5,10,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="AUTO" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="AUTO" />
                </Grid.RowDefinitions>
                <StackLayout Orientation="Vertical" Grid.Row="0" Grid.Column="0">
                    <Label FontSize="{StaticResource StandardLabelFontSize}" VerticalOptions="Center" 
                        HorizontalOptions="StartAndExpand" Text="{Binding DescrProvinciaCompletaUnbound}" />
                </StackLayout>
            </Grid>
        </StackLayout>
    </ContentView.Content>
</ContentView>